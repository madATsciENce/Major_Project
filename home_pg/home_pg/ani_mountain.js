const buttons = document.querySelectorAll('.filter-button');

buttons.forEach(button => {
  button.onclick = function() {
    location.href = 'minor project_anisha/ewns.html'; // replace with the actual URL
  }
});