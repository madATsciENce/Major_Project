const buttons = document.querySelectorAll('.filter-button');

buttons.forEach(button => {
  button.onclick = function() {
    location.href = 'Minor Project_Peulina/front page by anisha.html'; // replace with the actual URL
  }
});