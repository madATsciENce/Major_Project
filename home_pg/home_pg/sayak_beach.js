const buttons = document.querySelectorAll('.filter-button');

buttons.forEach(button => {
  button.onclick = function() {
    location.href = 'Minor_Project_sayak/1.html'; // replace with the actual URL
  }
});

